{
  "name": "bg-remover",
  "version": "1.0.0",
  "description": "Background removal application",
  "scripts": {
    "build": "netlify build",
    "dev": "netlify dev"
  },
  "dependencies": {
    "@netlify/plugin-python": "^1.0.1"
  },
  "devDependencies": {
    "netlify-cli": "^12.0.0"
  }
}
